model:
  name: gpt-4o
  model_id: gpt-4o
  hidden_dim: 0
  num_layers: 0
  eval_only: true
generation:
  num_runs: 5
  max_new_tokens: 16384
  seed: 42
  temperature: 0.6
paths:
  problems: data/datasets/selected_500_math.parquet
  pipeline:
    cot_traces: ${IRT_OUTPUTS_ROOT}/api/math/gpt-4o/cot_traces.jsonl
  analysis:
    cot_analysis: ${IRT_RESULTS_ROOT}/math/gpt-4o/cot_analysis/cot_analysis.parquet
